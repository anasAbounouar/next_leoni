name: Check iLovePDF API Status with PDF Conversion

on:
  workflow_dispatch:
  push:
  schedule:
    - cron: '0 0 * * *'  # Runs every day at midnight UTC

jobs:
  check-ilovepdf-api:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout Repository
      uses: actions/checkout@v3

    - name: Install Node.js
      uses: actions/setup-node@v3
      with:
        node-version: '22'

    - name: Install  dependencies
      run: |
        npm i @ilovepdf/ilovepdf-nodejs minimist
    - name: List directory (for debugging)
      run: ls -R /home/runner/work/next_leoni/next_leoni/public

    - name: Check API Status with Conversion
      id: check_api
      run: |
        node ./src/utils/ilovepdf_check.js --publicKey project_public_ad5f92aa89ba7edaa84be1e4652f2afd_Csbn2a9b833de935530a565ce276d04411547 --secretKey secret_key_f0bbddb93f13fd5cbf5c94ddded59592_F3oAqe22b73cee70dc1c7c59f87ce63dc3d28 --check-api

    - name: Handle Failure
      if: failure()
      run: |
        echo "API is down or out of points. Exiting."
        exit 1  # Ensure the job fails with exit code 1

    - name: Notify on Success
      if: success()
      run: |
        echo "iLovePDF API is operational and conversion successful."
